name: wf-prueba

on:
  push:
    branches:
      - master

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: |
          echo "Hola mundo!"
      - name: Dime la fecha
        run: date
      - run: ls -l